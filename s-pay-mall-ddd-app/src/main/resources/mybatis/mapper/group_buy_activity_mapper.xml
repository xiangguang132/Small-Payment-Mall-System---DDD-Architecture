<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.bugstack.infrastructure.dao.IGroupBuyActivityDao">

    <resultMap id="activityMap"
               type="cn.bugstack.infrastructure.dao.po.groupbuy.GroupBuyActivity">
        <id column="id" property="id"/>
        <result column="activity_id" property="activityId"/>
        <result column="activity_name" property="activityName"/>
        <result column="product_id" property="productId"/>
        <result column="discount_id" property="discountId"/>
        <result column="group_type" property="groupType"/>
        <result column="take_limit_count" property="takeLimitCount"/>
        <result column="target_count" property="targetCount"/>
        <result column="valid_time" property="validTime"/>
        <result column="status" property="status"/>
        <result column="start_time" property="startTime"/>
        <result column="end_time" property="endTime"/>
        <result column="tag_id" property="tagId"/>
        <result column="tag_scope" property="tagScope"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 联表查询扩展 resultMap：继承 activityMap，追加折扣表字段 -->
    <resultMap id="activityWithDiscountMap" type="cn.bugstack.infrastructure.dao.po.groupbuy.GroupBuyActivity"
               extends="activityMap">
        <result column="discount_name" property="discountName"/>
        <result column="market_plan" property="marketPlan"/>
    </resultMap>

    <select id="queryGroupBuyActivityByActivityId" parameterType="java.lang.Long"
            resultMap="activityMap">
        select id, activity_id, activity_name, product_id, discount_id,
               group_type, take_limit_count, target_count, valid_time,
               status, start_time, end_time, tag_id, tag_scope,
               create_time, update_time
        from group_buy_activity
        where activity_id = #{activityId}
    </select>

    <select id="queryByProductId" parameterType="java.lang.Long" resultMap="activityMap">
        select id, activity_id, activity_name, product_id, discount_id,
               group_type, take_limit_count, target_count, valid_time,
               status, start_time, end_time, tag_id, tag_scope,
               create_time, update_time
        from group_buy_activity
        where product_id = #{productId}
          and status = 1
        limit 1
    </select>

    <select id="queryGroupBuyActivityList" resultMap="activityMap">
        select id, activity_id, activity_name, product_id, discount_id,
               group_type, take_limit_count, target_count, valid_time,
               status, start_time, end_time, tag_id, tag_scope,
               create_time, update_time
        from group_buy_activity
    </select>

    <select id="countByTagId" parameterType="java.lang.String" resultType="long">
        select count(1)
        from group_buy_activity
        where tag_id = #{tagId}
    </select>

    <sql id="queryActivityPageByMarketPlan_condition">
        <where>
            <if test="marketPlan != null and marketPlan != ''">
                and d.market_plan = #{marketPlan}
            </if>
        </where>
    </sql>

    <select id="queryActivityPageByMarketPlan" resultMap="activityWithDiscountMap">
        select
            a.id, a.activity_id, a.activity_name, a.product_id,
            a.discount_id, a.group_type, a.take_limit_count, a.target_count,
            a.valid_time, a.status, a.start_time, a.end_time,
            a.tag_id, a.tag_scope, a.create_time, a.update_time,
            d.discount_name, d.market_plan
        from group_buy_activity a
        inner join group_buy_discount d on d.discount_id = a.discount_id
        <include refid="queryActivityPageByMarketPlan_condition"/>
        order by a.create_time desc
        limit #{offset}, #{limit}
    </select>

    <select id="countActivityPageByMarketPlan" resultType="long">
        select count(1)
        from group_buy_activity a
        inner join group_buy_discount d on d.discount_id = a.discount_id
        <include refid="queryActivityPageByMarketPlan_condition"/>
    </select>

</mapper>
