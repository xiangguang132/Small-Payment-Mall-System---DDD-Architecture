<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.bugstack.infrastructure.dao.IProductPromotionRuleDao">

    <resultMap id="dataMap" type="cn.bugstack.infrastructure.dao.po.promotion.ProductPromotionRule">
        <id column="id" property="id"/>
        <result column="product_id" property="productId"/>
        <result column="rule_type" property="ruleType"/>
        <result column="rule_code" property="ruleCode"/>
        <result column="priority" property="priority"/>
        <result column="stackable" property="stackable"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="queryProductPromotionRuleListByProductId" resultMap="dataMap">
        select id, product_id, rule_type, rule_code, priority, stackable, status, create_time, update_time
        from product_promotion_rule
        where product_id = #{productId} and status = 1
        order by priority asc
    </select>

</mapper>
