<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.bugstack.infrastructure.dao.IPayOrderItemDao">

    <resultMap id="dataMap" type="cn.bugstack.infrastructure.dao.po.payment.PayOrderItem">
        <id column="id" property="id"/>
        <result column="order_id" property="orderId"/>
        <result column="product_id" property="productId"/>
        <result column="product_name" property="productName"/>
        <result column="quantity" property="quantity"/>
        <result column="price" property="price"/>
        <result column="total_amount" property="totalAmount"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="batchInsert">
        insert into pay_order_item(order_id, product_id, product_name, quantity, price, total_amount, create_time, update_time)
        values
        <foreach collection="items" item="item" separator=",">
            (#{item.orderId}, #{item.productId}, #{item.productName}, #{item.quantity}, #{item.price}, #{item.totalAmount}, now(), now())
        </foreach>
    </insert>

    <select id="queryByOrderId" resultMap="dataMap">
        select id, order_id, product_id, product_name, quantity, price, total_amount, create_time, update_time
        from pay_order_item
        where order_id = #{orderId}
    </select>

    <select id="queryByOrderIds" resultMap="dataMap">
        select id, order_id, product_id, product_name, quantity, price, total_amount, create_time, update_time
        from pay_order_item
        where order_id in
        <foreach collection="orderIds" item="orderId" open="(" separator="," close=")">
            #{orderId}
        </foreach>
    </select>

</mapper>