<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.bugstack.infrastructure.dao.IProductCarouselDao">

    <resultMap id="dataMap" type="cn.bugstack.infrastructure.dao.po.product.ProductCarousel">
        <result column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="cover_img" property="coverImg"/>
        <result column="target_type" property="targetType"/>
        <result column="target_id" property="targetId"/>
        <result column="link_url" property="linkUrl"/>
        <result column="status" property="status"/>
        <result column="is_del" property="isDel"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, title, cover_img, target_type, target_id, link_url, status, is_del, create_time, update_time
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO product_carousel (title, cover_img, target_type, target_id, link_url, status, is_del)
        VALUES (#{title}, #{coverImg}, #{targetType}, #{targetId}, #{linkUrl}, #{status}, #{isDel})
    </insert>

    <update id="deleteById">
        UPDATE product_carousel SET is_del = 1 WHERE id = #{id} AND is_del = 0
    </update>

    <select id="queryById" resultMap="dataMap">
        SELECT <include refid="Base_Column_List"/>
        FROM product_carousel
        WHERE id = #{id} AND is_del = 0
    </select>

    <update id="update">
        UPDATE product_carousel
        SET title = #{title}, cover_img = #{coverImg}, target_type = #{targetType},
            target_id = #{targetId}, link_url = #{linkUrl}, status = #{status}
        WHERE id = #{id} AND is_del = 0
    </update>

    <select id="queryActiveList" resultMap="dataMap">
        SELECT <include refid="Base_Column_List"/>
        FROM product_carousel
        WHERE status = 1 AND is_del = 0
        ORDER BY id DESC
    </select>

    <select id="queryPage" resultMap="dataMap">
        SELECT <include refid="Base_Column_List"/>
        FROM product_carousel
        WHERE is_del = 0
        ORDER BY id DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countPage" resultType="long">
        SELECT COUNT(1) FROM product_carousel WHERE is_del = 0
    </select>

</mapper>
