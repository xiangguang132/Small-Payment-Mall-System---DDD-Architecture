<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.bugstack.infrastructure.dao.IProductDao">

    <insert id="insert" parameterType="cn.bugstack.infrastructure.dao.po.Product"
            useGeneratedKeys="true" keyProperty="id">
        insert into product
        (name, description, sku, category_id, status, price, is_del, create_time, update_time)
        values
        (#{name}, #{description}, #{sku}, #{categoryId}, #{status}, #{price}, #{isDel}, now(), now())
    </insert>

    <update id="deleteById" parameterType="java.lang.Long">
        update product
        set is_del = 1,
        update_time = now()
        where id = #{id}
        and is_del = 0
    </update>

    <select id="queryById" parameterType="java.lang.Long" resultType="cn.bugstack.infrastructure.dao.po.Product">
        select
        p.id,
        p.name,
        p.description,
        p.sku,
        p.category_id,
        pt.name as category_name,
        pt.description as category_description,
        pt.type_code as category_code,
        p.status,
        p.price,
        p.is_del,
        p.create_time,
        p.update_time
        from product p
        left join product_type pt on p.category_id = pt.id
        where p.id = #{id}
        and p.is_del = 0
    </select>

    <update id="update" parameterType="cn.bugstack.infrastructure.dao.po.Product">
        update product
        set name = #{name},
        description = #{description},
        sku = #{sku},
        category_id = #{categoryId},
        status = #{status},
        price = #{price},
        update_time = now()
        where id = #{id}
        and is_del = 0
    </update>

</mapper>
