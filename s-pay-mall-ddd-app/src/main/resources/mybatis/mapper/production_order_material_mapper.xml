<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.bugstack.infrastructure.dao.IProductionOrderMaterialDao">

    <resultMap id="productionOrderMaterialMap" type="cn.bugstack.infrastructure.dao.po.product.ProductionOrderMaterial">
        <id column="id" property="id"/>
        <result column="production_order_id" property="productionOrderId"/>
        <result column="material_id" property="materialId"/>
        <result column="material_quantity" property="materialQuantity"/>
        <result column="allocation_no" property="allocationNo"/>
        <result column="status" property="status"/>
        <result column="is_del" property="isDel"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insertBatch">
        insert into production_order_material
        (production_order_id, material_id, material_quantity, allocation_no, status, is_del, create_time, update_time)
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.productionOrderId}, #{item.materialId}, #{item.materialQuantity}, #{item.allocationNo}, #{item.status}, #{item.isDel}, #{item.createTime}, #{item.updateTime})
        </foreach>
    </insert>

    <select id="queryByProductionOrderId" parameterType="java.lang.Long" resultMap="productionOrderMaterialMap">
        select id, production_order_id, material_id, material_quantity, allocation_no, status, is_del, create_time, update_time
        from production_order_material
        where production_order_id = #{productionOrderId}
          and is_del = 0
        order by id asc
    </select>

    <update id="updateAllocationNo">
        update production_order_material
        set allocation_no = #{allocationNo},
        status = #{status},
        update_time = now()
        where id = #{id}
        and is_del = 0
    </update>
</mapper>
