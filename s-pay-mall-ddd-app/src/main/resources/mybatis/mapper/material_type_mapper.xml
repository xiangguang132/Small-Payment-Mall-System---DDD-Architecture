<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.bugstack.infrastructure.dao.IMaterialTypeDao">

    <insert id="insert" parameterType="cn.bugstack.infrastructure.dao.po.MaterialType"
            useGeneratedKeys="true" keyProperty="id">
        insert into material_type
        (parent_id, name, description, type_code, sort, status, is_del, create_time, update_time)
        values
        (#{parentId}, #{name}, #{description}, #{typeCode}, #{sort}, #{status}, #{isDel}, #{createTime}, #{updateTime})
    </insert>

    <update id="deleteById" parameterType="java.lang.Long">
        update material_type
        set is_del = 1,
            update_time = now()
        where id = #{id}
          and is_del = 0
    </update>

    <select id="queryById" parameterType="java.lang.Long" resultType="cn.bugstack.infrastructure.dao.po.MaterialType">
        select
        id,
        parent_id,
        name,
        description,
        type_code,
        sort,
        status,
        is_del,
        create_time,
        update_time
        from material_type
        where id = #{id}
          and is_del = 0
    </select>

    <select id="queryByTypeCode" parameterType="java.lang.String" resultType="cn.bugstack.infrastructure.dao.po.MaterialType">
        select
        id,
        parent_id,
        name,
        description,
        type_code,
        sort,
        status,
        is_del,
        create_time,
        update_time
        from material_type
        where type_code = #{typeCode}
          and is_del = 0
    </select>

    <update id="update" parameterType="cn.bugstack.infrastructure.dao.po.MaterialType">
        update material_type
        set parent_id = #{parentId},
            name = #{name},
            description = #{description},
            type_code = #{typeCode},
            sort = #{sort},
            status = #{status},
            update_time = now()
        where id = #{id}
          and is_del = 0
    </update>

    <select id="countByParentId" parameterType="java.lang.Long" resultType="java.lang.Long">
        select count(1)
        from material_type
        where parent_id = #{parentId}
          and is_del = 0
    </select>

</mapper>
